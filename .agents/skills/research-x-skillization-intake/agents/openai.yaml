interface:
  display_name: "research_x Skillization Intake"
  short_description: "Route recurring Codex behavior and Skill invocation into AGENTS, skills, hooks, plugins, MCP, or docs without Markdown bloat."
  default_prompt: "Use this when adding recurring Codex behavior, improving native Skill invocation or Skill Router Preflight, fixing missed automatic Skill firing, reducing AGENTS bloat, or deciding between repo Skills, AGENTS, plugins, MCP, docs, automation, hooks, or prompt-only rules."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
