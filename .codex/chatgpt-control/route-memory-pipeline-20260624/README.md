# Route Memory Pipeline GPT Pro Package

Date: 2026-06-24

This package is a ChatGPT/GPT Pro consultation input for reviewing whether
`research_x` already has enough AGENTS/Skill/WBS/PDG/Pointer structure to support
an always-on Codex operation pipeline that reuses prior success/failure routes
and uses exploration sidecars for unknown blockers.

The package is not evidence, not an active tracker, and not permission to call
providers or enable hooks/plugins/MCPs.

Primary prompt:

- `PROMPT.md`

Packaged context is copied under `package/` when the ZIP is built.
