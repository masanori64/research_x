interface:
  display_name: "research_x Observability Review"
  short_description: "Review black-box app/CLI/workflow state and add progress, trace, route, budget, evidence, or gap visibility."
  default_prompt: "Use this when the user says app, CLI, job, progress, route decisions, evidence, budget, media, OCR, or workflow state is unclear or black-box."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
