interface:
  display_name: "research_x Context Budget"
  short_description: "Build context packs, offloads, and compression decisions without losing source-bundle evidence."
  default_prompt: "Use this when research_x work mentions context budget, context pack, compression, offload, Headroom, handoff capsules, long traces, token limits, or preserving evidence under a prompt/output budget."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
