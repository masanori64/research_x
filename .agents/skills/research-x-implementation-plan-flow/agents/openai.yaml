interface:
  display_name: "research_x Implementation Plan Flow"
  short_description: "Turn GPT/X/source-candidate reviews into gated local-first implementation priority flows."
  default_prompt: "Use this when converting GPT Pro/X URL analysis, source-intake results, candidate matrices, deferred-item reviews, or project-usability reviews into a reproducible research_x implementation-priority flow with phases, gates, stop conditions, and local-first execution order."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
