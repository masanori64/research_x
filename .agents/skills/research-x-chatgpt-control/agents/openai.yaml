interface:
  display_name: "research_x ChatGPT Control"
  short_description: "Use visible, user-directed ChatGPT web consultation as a Codex helper without making it evidence."
  default_prompt: "Use this when Codex should consult or control a visible ChatGPT web session through codex-chatgpt-control, while preserving blockers, user approval, redaction, and citation-excluded output policy."

policy:
  allow_implicit_invocation: true

dependencies:
  tools: []
